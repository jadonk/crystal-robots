image: $CI_REGISTRY/jkridner/crystal
build:
  stage: build
  script:
    - crystal build src/crystal-robots.cr
    - crystal build src/examples/rabbit.cr
  artifacts:
    paths:
      - crystal-robots
      - rabbit

test:
  stage: test
  script:
    - crystal spec
    - crystal tool format --check
    - ./crystal-robots -v
    - ./crystal-robots
    - ./rabbit

pages:
  stage: deploy
  script:
    - git config --global --add safe.directory /builds/jkridner/crystal-robots
    - crystal docs -o public --project-version `git show --format=%h -s` src/crystal-robots.cr lib/crystal-robots/src/crystal-robots.cr
  artifacts:
    paths:
      - public
